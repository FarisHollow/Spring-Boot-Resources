# Spring-Boot-Resources


About MongoDB: There are Collections, 
                             In Collections (Table):
                                        -fields(Column)
                                            -documents (Row)
